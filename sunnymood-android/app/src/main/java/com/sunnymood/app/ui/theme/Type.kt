package com.sunnymood.app.ui.theme

import androidx.compose.material3.Typography

/**
 * 字体排印：使用 Material3 默认 + 系统字体。
 * 刻意不引入自定义字体——ColorOS 上自然获得 OPPO Sans，观感与系统一致（§4.7）。
 */
val SunnyTypography = Typography()
