# 心情记录 App 开发文档

> **名称**：心晴 SunnyMood（原代号 MoodLog） · **版本**：v1.6 · **日期**：2026-09-10
> **状态**：核心决策已确认，冻结为开发基线（v1.6 新增 WebDAV 云同步并提前至 P0、放弃微信登录，见 §9 变更记录）

---

## 1. 项目概述

### 1.1 产品定位

一款**零负担**的心情 + 身体状态记录 Android 应用：在手机桌面小组件上**点一下图标**记录心情，顺手可勾选身体感觉（累 / 困 / 不想动……），每月自动生成心情报告与个性化建议。

### 1.2 设计理念

心情/日记类 App 流失的最大原因是**记录成本高**（解锁 → 打开 → 输入 → 保存）。本产品把记录动作压缩为**桌面一次点击**；而记录本身没有意义，**看见趋势、获得建议**才有意义，因此月度报告与建议是第二核心。

**为什么加身体感觉**：对职场人而言，"累、困、不想动"是比情绪更早、更诚实的身体信号；且心情 × 身体的**关联分析**（如"疲惫的日子心情平均低 1.4 分"）是单一维度给不了的洞察价值。

### 1.3 成功标准

- 记录动作 ≤ 1 次点击、≤ 0.5 秒（身体感觉为可选追加，不阻塞主路径）
- 无账号、无广告，数据默认纯本地；可选开启**端到端加密云同步**（WebDAV，数据只存用户自己的网盘，网盘与开发者均无法解密）
- 用户愿意持续使用，来自报告带来的"被看见 + 有建议"的价值感

---

## 2. 功能需求

### 2.1 核心用户故事（P0）

1. 我点一下桌面小组件上的心情图标，**记录瞬间完成**；屏幕弹出轻面板显示"已记录 😄"，我可以顺手勾选今天的身体感觉（可多选），也可以直接划走不管
2. 我心情有变化时随时再点，一天可以记录多次
3. 我打开 App，能看到今天记了几条、几点记的、当时的身体感觉
4. 我点错了图标，在时间线里点那条记录，就能**改心情、改身体感觉，或直接删掉**
5. 我能在日历上按颜色回顾任意一天的心情
6. 我打开报告页，能看到本月的心情评分、分布、趋势、身体感觉排行，以及**给我的一些建议**
7. 我第一次使用时，App 会引导我如何把小组件添加到桌面
8. 我可以在设置里把全部数据导出成文件，换机或备份不丢数据
9. 我第一次打开 App 就能看到《隐私政策》，明确数据默认只存本机；我随时可以清空全部数据
10. 我可以在设置里绑定**我自己的网盘**（坚果云 / NextCloud 等）开启云同步：数据**加密后**才上传，网盘服务商和 App 开发者都看不到内容
11. 我卸载重装（或换手机）后，绑定同一网盘账号并输入同步口令，全部记录**完整恢复**

### 2.2 功能清单

**P0 —— MVP 必做**

| 模块 | 功能 | 说明 |
|---|---|---|
| 桌面小组件 | 一键记录 | 7 个心情图标，点击即记录；震动反馈 + 当日状态刷新 |
| | 今日状态 | 小组件上显示今日已记录条数 / 最新心情 |
| | 两种尺寸 | 4x1 紧凑版 / 4x2 大图标版（**主推**，7 图标触控更从容） |
| 记录 | 身体感觉（可选追加） | 点心情后弹轻面板，**多选**固定词表（10 项，见 4.3）；可跳过；设置可关闭面板回到纯一键 |
| | App 内记录 | 与 Widget 等效的记录入口（保底，防止未添加小组件） |
| | 今日时间线 | 列出今日每条记录：时间 + 心情 + 身体感觉标签 |
| | 编辑与删除 | 时间线 / 日历中点任意条目：**改心情等级、改身体感觉、删除**（误触补救，数据准确是报告的地基） |
| 回顾 | 日历视图 | 月历网格，格子按当日均分着色（7 档色阶） |
| 报告 | 月度报告 | 评分 / 分布 / 趋势 / 洞察 / 文字总结 |
| | 心情 × 身体关联分析 | "出现『疲惫』的日子，平均心情比其他日子低 X 分" |
| | 月度建议 | 本地规则引擎生成 2~3 条可执行建议 + 触发式关怀提示 |
| 引导 | 添加小组件引导 | 首次启动检测小组件是否已添加，无则分步图文引导 |
| 数据 | 一键导出 | CSV / JSON 通过系统分享导出（离线备份兜底） |
| | **云同步（WebDAV）** | 绑定用户自选网盘（坚果云 / NextCloud 等），**端到端加密**后才上传；默认关闭、开启才联网；卸载重装 / 换机可完整恢复（技术方案见 3.10） |
| 系统 | 深色模式 | 跟随系统（含小组件深色适配） |
| 合规 | 隐私政策 | 首次启动展示《隐私政策》（不默认勾选），同意后进入；设置页常驻入口（见 3.8） |
| | 清空全部数据 | 设置页一键清空所有记录（二次确认），保障用户删除权 |
| 质量 | 诊断日志 | 本地记录崩溃与关键操作日志（**不含心情数据内容**），设置页一键导出，用于真机排障（见 3.9） |

**P1 —— 1.x 版本**

| 模块 | 功能 | 说明 |
|---|---|---|
| 隐私 | 应用锁 | 生物识别 / 锁屏 PIN（BiometricPrompt），打开 App 时验证 |
| 快捷入口 | 长按图标快捷方式 | 静态 shortcuts：长按 App 图标直达 7 档心情一键记录 |
| 快捷入口 | 快捷设置磁贴 | 下拉快捷面板中的"记一笔"磁贴（TileService），任何时候下拉即记 |
| 记录增强 | 备注 / 自由文字 | 记录后**可选**追加，不增加点击摩擦 |
| 词表 | 身体感觉自定义 | 设置中增删/排序身体感觉词表（MVP 为固定 10 项） |
| 报告增强 | 导出分享报告图片、周报 / 年报、星期 & 时段分析 | |
| 小组件 | 2x2 纯图标尺寸、样式切换、4x2 下半区迷你周趋势 | |
| 提醒 | 每日固定时间提醒、每月 1 号"上月报告已生成"通知 | |
| 数据 | 手动导入 | CSV / JSON 导入换机恢复（导出已在 P0 提供） |

**P2 —— 远期**

- 自定义心情影响因素问卷（如"睡眠如何"）
- 独立身体感觉记录（不依附心情条目）
- 大模型润色报告与建议（云同步已提前至 P0；多设备由其同步模型天然支持，见 3.10）
- 小组件主题样式商店

### 2.3 非功能需求

| 维度 | 要求 |
|---|---|
| 性能 | Widget 点击 → 记录落库 ≤ 300ms；报告页打开 ≤ 500ms |
| 隐私 | 数据默认仅存本地、零网络行为；INTERNET 权限仅服务于**用户主动开启**的云同步（端到端加密，见 3.10）；导出走系统分享，不申请存储权限 |
| 兼容 | Android 8.0（API 26）+，覆盖主流国产 ROM |
| 体积 | APK ≤ 10MB |
| 耗电 | 无后台常驻服务，仅点击瞬间工作，零额外耗电 |
| 无障碍 | 图标可被 TalkBack 读出；色阶 + 表情形状双重编码，色弱友好 |
| 伦理 | 建议为通用生活方式建议，**不做医疗诊断**；长期低落触发关怀提示（见 3.6.3） |
| 合规 | 符合工信部 App 个人信息保护要求与《个人信息保护法》最小必要原则（逐项对照见 3.8） |

---

## 3. 技术方案

### 3.1 总体选型

| 项 | 选择 | 理由 |
|---|---|---|
| 平台 | 原生 Android（Kotlin） | 桌面小组件是平台深度功能，跨端框架（Flutter/RN）需大量原生桥接代码，无收益 |
| UI | Jetpack Compose | 声明式 UI，动画友好，官方主推 |
| 小组件 | RemoteViews + AppWidgetProvider | 纯点击交互场景下最稳、兼容性最好（对比见 3.2） |
| 数据 | Room + Kotlin Flow | 官方 ORM；Flow 驱动 UI 自动刷新 |
| 网络（仅云同步用） | OkHttp | 成熟 HTTP 客户端，直接发 WebDAV 标准请求，无需专门 WebDAV 库；开源传输库，**非数据收集类 SDK** |
| 加密 | Android 内置 javax.crypto | PBKDF2 口令派生 + AES-256-GCM，零新增依赖（见 3.10） |
| 图表 | Vico（Compose 图表库） | 与 Compose 无缝集成 |
| 架构 | 单 Activity + MVVM + Repository | Google 官方推荐架构，小项目轻量实现 |
| 构建 | Gradle（KTS）+ Version Catalog | 现代标准 |

### 3.2 Widget 方案对比：为什么选 RemoteViews 而不是 Glance

| | RemoteViews（**选用**） | Jetpack Glance |
|---|---|---|
| 兼容性 / 可预测性 | 最可靠，出问题好排查 | 底层仍是生成 RemoteViews，多一层封装 |
| 表达力 | 弱（但我们只需要文本 + 图片 + 点击） | 强（Compose 写法） |
| 额外依赖 | 0 | +1 个库 |
| 学习资料 | 丰富 | 相对少 |

**结论**：我们的 Widget 交互极简（7 个点击目标 + 状态文字），RemoteViews 完全胜任且坑最少。

### 3.3 工程结构

```
app/src/main/java/com/sunnymood/app/
├── SunnyMoodApp.kt                # Application
├── diagnostic/
│   └── Diagnostics.kt             # 本地诊断日志：崩溃捕获 + 滚动日志 + 导出（见 3.9）
├── MainActivity.kt                # 单 Activity（主界面 3 Tab）
├── widget/
│   ├── MoodWidgetProvider.kt      # 小组件生命周期（onUpdate 绑定点击）
│   ├── MoodClickReceiver.kt       # 纯点击模式的广播接收器
│   └── WidgetState.kt             # 小组件视图状态计算
├── ui/
│   ├── record/
│   │   └── RecordPanelActivity.kt # 轻面板：记录确认 + 身体感觉多选；复用作编辑面板
│   ├── theme/                     # 主题 / 深色模式
│   ├── home/                      # 首页：今日时间线 + 记录按钮 + 周趋势
│   ├── calendar/                  # 日历
│   ├── report/                    # 月度报告
│   └── settings/                  # 设置
├── data/
│   ├── db/                        # MoodRecord / DAO / AppDatabase
│   ├── MoodRepository.kt
│   └── Exporter.kt                # CSV / JSON 序列化 + 系统分享（P0）
├── sync/                          # 云同步（P0，见 3.10；默认关闭）
│   ├── WebDavClient.kt            # OkHttp 封装：PUT / GET / PROPFIND / MKCOL
│   ├── SyncCrypto.kt              # 口令派生（PBKDF2）+ AES-256-GCM 加解密
│   ├── SyncEngine.kt              # 快照序列化 / LWW+墓碑合并 / 触发调度（去抖）
│   └── SyncSettingsStore.kt       # 同步配置与口令（EncryptedSharedPreferences）
├── report/
│   ├── MoodReportGenerator.kt     # 报告统计（纯 Kotlin，便于单元测试）
│   └── AdviceEngine.kt            # 建议规则引擎（纯 Kotlin，便于单元测试）
└── util/
```

### 3.4 数据模型

```kotlin
@Entity(tableName = "mood_records", indices = [Index("timestamp")])
data class MoodRecord(
    @PrimaryKey val id: String = UUID.randomUUID().toString(), // UUID：多设备同步不冲突（见 3.10）
    val moodLevel: Int,            // 1..7（1=非常差，7=非常好）
    val timestamp: Long,           // 毫秒时间戳
    val bodyFeelings: String? = null, // 身体感觉，JSON 数组，如 ["疲惫","困倦"]；P0 启用
    val note: String? = null,      // 备注（P1 启用，表结构先行预留，免迁移）
    val tags: String? = null,      // 标签（P1 启用）
    val updatedAt: Long = 0,       // 最后修改时间（创建时 = timestamp）；云同步 LWW 合并依据
    val deleted: Boolean = false   // 软删除墓碑：让"删除"可随同步传播（见 3.10）；查询一律过滤
)
```

- **一天可多次记录**：每条独立存储，按 `timestamp` 聚合出日/月统计
- **身体感觉以 JSON 字符串存在记录上**：月报一次 `queryRange` 取回全月数据后由纯函数解析统计，数据量小（一年 ≤ 万条），无需独立表与额外查询，简单可靠
- 身体感觉 MVP 为固定词表（代码常量），P1 自定义时引入词表实体
- **边界约定**：身体感觉只记"有无"，不记程度（程度量化留给 P2 问卷）；编辑可改等级与身体感觉，**不改时间戳**（为过去补记属 P1"手动补录"范畴）
- **时区**：所有按日/按月统计基于设备**本地时区**的 00:00 日界切分
- **主键为 UUID 字符串**（v1.6 起）：Long 自增 id 在多设备同步时必然撞号（两台设备都从 1 开始自增），UUID 天然不冲突；万条级数据量下性能差异无感
- **软删除（deleted 墓碑）**：为让"删除"能随云同步传播到其他设备 / 重装后的本机，删除不做物理 DELETE 而是置墓碑；所有查询与统计过滤 `deleted = 0`；「清空全部数据」提供独立选项决定是否连带清空云端（见 3.10）
- DAO 关键方法：

```kotlin
@Query("SELECT * FROM mood_records WHERE deleted = 0 AND timestamp BETWEEN :from AND :to ORDER BY timestamp")
fun queryRange(from: Long, to: Long): Flow<List<MoodRecord>>   // 报告 / 日历（过滤墓碑）

@Query("SELECT * FROM mood_records WHERE deleted = 0 AND timestamp >= :todayStart ORDER BY timestamp DESC")
fun observeToday(todayStart: Long): Flow<List<MoodRecord>>     // 今日时间线 / Widget 状态

@Update
suspend fun update(record: MoodRecord)                         // 编辑：改等级 / 身体感觉（同时刷新 updatedAt）

@Query("UPDATE mood_records SET deleted = 1, updatedAt = :now WHERE id = :id")
suspend fun deleteById(id: String, now: Long)                  // 软删除：置墓碑供同步传播（见 3.10）
```

### 3.5 核心流程：桌面一键记录 + 可选身体感觉

**默认（面板模式）**：

```
[桌面小组件] 点击心情图标
   │  PendingIntent.getActivity → 启动透明轻面板（携带 moodLevel）
   ▼
RecordPanelActivity（透明主题，即时弹出）
   │ 1. onCreate 立即写入 Room —— 记录此刻已完成 ✅
   │ 2. 震动反馈 + 大图标显示"已记录 😄 很好"
   │ 3.（可选，不阻塞）勾选身体感觉：疲惫 / 困倦 / 不想动 …（多选）
   │ 4. 点"完成"保存勾选；或直接划走/点外部关闭 —— 未选也不影响记录
   ▼
关闭面板 → 刷新小组件（最新心情高亮 + 今日记录数）
```

**纯点击模式**（设置开关，默认关闭）：

```
点击图标 → PendingIntent.getBroadcast → MoodClickReceiver 直接落库（v1.0 方案）→ 刷新小组件
```

技术要点与期望管理：

- **为什么用 getActivity 直启面板**：Android 10+ 限制后台 Activity 启动，但 Widget 点击的 PendingIntent 属于豁免场景；先广播再 startActivity 的两跳方案不可靠
- Android 12+：PendingIntent 必须加 `FLAG_IMMUTABLE`
- **平台限制**：Widget 本身不支持复杂动画；轻面板补位了"确认 + 追加"的交互，主路径仍是"点一下就完事"
- **摩擦控制**：面板可跳过、可关闭、设置可彻底关闭回到纯一键——三层保险保住"零负担"底线
- 无后台常驻：点击由系统即时唤醒，风险低；需在主流 ROM 上覆盖测试（见 §6）

### 3.6 月度报告与建议

数据流：`Repository 按月查询 → MoodReportGenerator（统计）→ AdviceEngine（建议）→ ReportScreen 渲染`
（报告为打开时实时计算；P1 增加每月 1 号通知提醒查看上月报告）

#### 3.6.1 统计指标

| 指标 | 算法 |
|---|---|
| 月评分 | AVG(moodLevel)，保留 1 位小数（1.0 ~ 7.0） |
| 记录覆盖率 | 有记录的天数 ÷ 当月天数 |
| 心情分布 | GROUP BY moodLevel 计数 |
| 日趋势 | 按日 AVG + 7 日移动平均 |
| 环比 | 与上月月评分之差 |
| 洞察 | 最快乐的一天 / 心情最好的星期几 / 最长连续记录天数 |
| 身体感觉排行 | 解析 bodyFeelings 计数，取 Top 3 及出现天数 |
| 心情 × 身体关联 | 对每个 Top 感觉：出现日均分 vs 未出现日均分之差 |

#### 3.6.2 建议引擎（本地规则，无网络）

按优先级排序，每月最多展示 **3 条**，规则示例：

| 触发条件 | 建议示例 |
|---|---|
| 疲惫/困倦/没睡好 合计出现 ≥ 40% 记录天数 | "本月 12 天出现疲惫或困倦，睡眠可能是主要消耗源，试试固定就寝时间" |
| 某身体感觉日的日均分显著低于整体（差 ≥ 1.0） | "出现『不想动』的日子心情平均低 1.6 分，留意工作强度，主动安排恢复时间" |
| 周一均分比周内均值低 ≥ 1.0 | "周一心情明显偏低，周日晚提前做点轻松的准备，能明显降低周一的启动成本" |
| 正面感觉与高心情共现 | "精力充沛的日子你的心情也更好（平均 6.2 分），保持当前的节奏" |
| 记录覆盖率 < 50% | "本月有 18 天没有记录，数据越连续，建议越准——试试把小组件放到顺手的位置" |

#### 3.6.3 伦理与关怀边界 ⚠️

- 所有建议均为**基于统计的通用生活方式建议**，不构成医疗诊断，报告页底部固定显示免责说明
- **触发式关怀**：连续 ≥ 5 天日均分 ≤ 2 且伴随高频负面身体感觉时，报告页顶部显示关怀卡片：建议休息、与信任的人聊聊，并附**全国心理援助热线 12356**
- 措辞规范：避免评判性表述（不说"你情绪不稳定"，说"这段时间你的状态偏低"）

### 3.7 权限与隐私

| 权限 | 用途 | 级别 |
|---|---|---|
| VIBRATE | 点击反馈 | 普通权限，免申请 |
| INTERNET | **云同步（P0）**：连接用户自选的 WebDAV 网盘 | 普通权限，安装即授予、无弹窗；**同步默认关闭，未开启时 App 零网络请求**（飞行模式 / 抓包可验证） |
| USE_BIOMETRIC（P1） | 应用锁 | 运行时由系统生物识别弹窗处理 |
| POST_NOTIFICATIONS（P1） | 每日提醒 / 月报通知 | 危险权限，运行时申请 |
| 存储权限 | **不申请** | 导出通过系统 SAF（`ACTION_CREATE_DOCUMENT`）+ FileProvider 分享实现，零权限 |

数据默认全部本地；卸载即清空本地 → **云同步为主备份**（用户主动开启，端到端加密）+ **一键导出为离线兜底**，设置页显著提示。

**权限申请三原则**（工信部"强制索权/频繁索权"整治项的落地）：

1. **场景化触发**：权限只在对应功能首次使用时申请（如 P1 通知权限，在用户主动开启"每日提醒"时才申请）
2. **拒绝不阻塞**：任何权限被拒绝都不影响核心记录功能，App 继续完整可用
3. **拒绝不重弹**：用户拒绝后不再频繁重复弹窗申请，仅在设置页提供手动开启入口

### 3.8 信息安全与合规（工信部要求对照）

**合规依据**：《个人信息保护法》《网络安全法》《App违法违规收集使用个人信息行为认定方法》《常见类型移动互联网应用程序必要个人信息范围规定》，以及工信部 App 侵害用户权益专项整治要求。

**架构级合规优势**：本 App 无账号体系、**零数据类第三方 SDK**（无统计 / 广告 / 推送 / 登录 SDK；仅引入 OkHttp 开源 HTTP 传输库，且只服务于用户主动开启的云同步，不涉及任何数据收集）；数据默认不离开设备，唯一的网络行为是用户**主动绑定网盘并开启**的云同步，且为端到端加密——服务器只见密文，网盘与开发者均无法解密，从源头控制个人信息风险。

**逐项对照**：

| 整治要点 | 监管要求 | 心晴的应对 | 状态 |
|---|---|---|---|
| 违规收集 | 公开收集使用规则 | 首启展示《隐私政策》（不默认勾选同意），同意后进入；设置页常驻入口 | 🔨 P0 实现 |
| 超范围收集 | 最小必要原则 | 仅存储用户主动记录的心情 / 身体感觉数据；不采集设备标识、位置、通讯录等任何其他信息 | ✅ 设计满足 |
| 数据上传 | 明示传输行为 | 默认零网络行为；开启云同步后也仅上传**端到端加密密文**至**用户自选**网盘（如坚果云），隐私政策明示；无任何向开发者服务器的传输 | ✅ |
| 强制 / 频繁索权 | 拒绝授权不得影响使用 | 权限清单仅 VIBRATE（普通权限，免申请）；P1 通知权限遵循"三原则" | ✅ |
| 自启动 | 不得频繁自启动 / 关联启动 | 无自启动与关联启动配置、无后台常驻服务 | ✅ |
| 删除权 | 提供删除途径 | 单条编辑 / 删除（P0）+ 设置页**一键清空全部记录**（P0，二次确认）；无账号故无注销问题 | 🔨 P0 实现 |
| 可携权 | 提供数据导出 | 一键导出 CSV / JSON（P0，SAF 免存储权限） | 🔨 P0 实现 |
| 欺诈误导 | 不得默认勾选 / 误导捆绑 | 隐私政策弹窗不预选同意；无捆绑下载与默认勾选项 | 🔨 P0 实现 |
| 投诉渠道 | 公布联系方式 | 《隐私政策》内提供开发者联系方式（占位邮箱） | 🔨 P0 实现 |

**《隐私政策》内容要点**（App 内页，M4 实现）：收集范围（仅记录数据、本地存储）、**不收集清单**（设备信息 / 位置 / 通讯录等一概不收集）、权限用途说明（含 INTERNET 仅用于用户开启的云同步）、数据安全（默认本地存储 / 卸载即删 / 导出文件由用户自行管理）、**云同步专章**（端到端加密方式、数据存于用户自选网盘、开发者与网盘均无法解密、同步口令丢失不可恢复的后果）、用户权利（改 / 删 / 清空 / 导出）、未成年人声明与开发者联系方式。

**分发合规提示**：仅自装 APK 使用**不涉及备案**；若上架国内应用商店，须完成 **App 备案（ICP，2023 年起施行）**与软件著作权登记（见 §7）。

### 3.9 可观测性：本地诊断日志（零网络下的排障方案）

App 不做任何自动上传（隐私优先：网络仅服务于用户**主动开启**的云同步，崩溃数据也概不例外）——为保障真机排障效率，采用**本地诊断日志**方案：

- **崩溃捕获**：`Thread.setDefaultUncaughtExceptionHandler` 捕获未处理异常，堆栈写入本地日志后交还系统默认处理
- **记录范围**：崩溃堆栈 + 关键操作（记录 / 编辑 / 删除 / 导出）的时间戳与操作类型；**不记录心情数值与身体感觉内容**，兼顾排障与隐私
- **存储**：`filesDir/logs/`，按天滚动，保留 7 天，体积上限约 1MB
- **导出**：设置页「导出诊断日志」→ 打包 zip 走系统分享（与数据导出同机制，SAF 免权限）
- **归属**：P0，随 M1 交付（开发期排障刚需）
- **与网络的关系**：即使 v1.6 起为云同步引入 INTERNET 权限，诊断日志依然**仅本地存储与导出、绝不上传**，隐私优先原则不变

### 3.10 云同步：WebDAV + 端到端加密（P0，v1.6 新增）

**目标**：卸载重装 / 换机后数据可完整恢复；**零自建服务器、零账号体系、零数据类第三方 SDK**——数据加密后存放在**用户自己的网盘**，网盘服务商与 App 开发者均无法解密。

**为什么是 WebDAV 而不是微信登录**：微信开放平台移动应用仅对企业主体开放（需营业执照 + 认证费），个人开发者无法申请，属硬门槛；且第三方登录 SDK 与"零数据类 SDK"基线冲突。WebDAV 由用户自带存储（坚果云 / NextCloud / 群晖等），App 只做加密上传与下载，责任边界干净、无任何企业资质要求。

**总体链路**：

```
上传：Room 全量 → JSON 快照 → AES-256-GCM 加密（密钥由同步口令派生）→ HTTPS PUT → 用户网盘 /sunnymood/backup.json.enc
下载：HTTPS GET → 解密（需口令）→ 与本地合并（UUID 对齐 · LWW · 墓碑）→ 写回 Room → UI 自动刷新
```

| 子项 | 方案 |
|---|---|
| 协议与服务 | 标准 WebDAV；**仅 HTTPS**（`usesCleartextTraffic=false`，禁明文）；只用 PUT / GET / PROPFIND / MKCOL 四个最基础方法，兼容性最好 |
| 推荐服务 | 坚果云（国内主流，免费档每月 1GB 上传流量，快照仅 MB 级绰绰有余；使用账户设置中生成的**应用密码**而非登录密码，绑定引导内嵌图文步骤）；亦支持 NextCloud / 群晖等自建 |
| 端到端加密 | 用户设置**同步口令** → PBKDF2-HMAC-SHA256（随机 salt，迭代次数按真机校准至派生耗时约 100~200ms）→ 256 位密钥 → AES-256-GCM（每次上传随机 IV）；口令与密钥**永不上传**，云端与开发者只见密文 |
| 口令存储 | 本机以 EncryptedSharedPreferences 加密保存（支持自动同步）；卸载即随之清除，重装恢复时需重新输入 |
| 快照格式 | 单文件全量 JSON（一年万条 ≈ 1~2MB，无需增量协议）；文件头带 schema 版本号（v1），字段演进向后兼容 |
| 合并规则 | 按 UUID 对齐 → 双方都有取 `updatedAt` 新者（LWW）→ 仅一方有则采纳 → 删除经 `deleted` 墓碑传播；该模型天然支持多设备先后使用 |
| 触发时机 | 默认自动：记录 / 编辑 / 删除后 30 秒去抖 + App 启动时；可设为纯手动（设置页「立即同步」）；无后台常驻 |
| 失败处理 | 即时提示原因（网络 / 认证失败 / 解密失败），下次触发自动重试；不做后台重试服务（"无常驻服务"原则不变） |
| 口令丢失 | 云端备份无法解密（端到端加密的必然代价），**本地数据不受影响**；开启时强制二次输入 + 明示后果 + 建议抄写备份 |
| 清空联动 | 「清空全部数据」提供选项：仅清本地（云端保留，可再恢复）或同时删除云端备份 |
| 默认状态 | **同步默认关闭**：未绑定网盘前 App 发起的网络请求数为 0（飞行模式 / 抓包可验证） |

**对既有架构的连锁调整**（均已落入本文档相应章节）：

1. INTERNET 权限：不申请 → **P0 申请**，用途单一（§3.7）
2. §3.8 合规对照「数据上传」行改写，隐私政策新增**云同步专章**
3. 数据模型：主键改 **UUID**、新增 `updatedAt` / `deleted`（§3.4）——同步合并与删除传播的地基
4. 里程碑新增 **M5 云同步**（3~5 天），MVP 完成线后移至 M5（§5）
5. 新增 **M5 UAT 验收清单**（§10）

---

## 4. UI/UX 设计

### 4.1 心情分级（已确认：7 级）

| 等级 | 含义 | 表情 | 主题色 |
|---|---|---|---|
| 7 | 非常好 | 大笑 | 翠绿 |
| 6 | 很好 | 微笑 | 浅绿 |
| 5 | 还不错 | 含笑 | 黄绿 |
| 4 | 一般 | 平静 | 琥珀 |
| 3 | 有点低 | 微沉 | 浅橙 |
| 2 | 不太好 | 皱眉 | 橙红 |
| 1 | 非常差 | 哭 | 玫红 |

图标实现：**自绘矢量图**（VectorDrawable）而非系统 emoji —— 保证所有设备渲染一致、颜色可控。色阶 + 表情形状双重编码，色弱用户也能区分。

### 4.2 桌面小组件（MVP 提供 4x1 / 4x2 两种尺寸）

**4x1 紧凑版**（两行布局）：

```
┌────────────────────────────────────────────┐
│ 今天 · 周四 · 已记 2 条            （小字） │
│  😞   😕   🙁   😐   ☺   🙂   😄          │
└────────────────────────────────────────────┘
```

**4x2 大图标版（主推）**：

```
┌────────────────────────────────────────────┐
│ 今天 · 周四 · 已记 2 条          最新 😄    │
│                                            │
│    😞    😕    🙁    😐    ☺    🙂    😄  │
│                                            │
└────────────────────────────────────────────┘
```

- 7 图标等宽分布，今日最新心情的图标微高亮
- 4x1 下图标约 42dp（略低于 48dp 无障碍标准），因此**主推 4x2**；4x2 下半区留给 P1 迷你周趋势
- **深色适配**：RemoteViews 使用夜间资源限定符（`values-night`），系统深色 / 深色壁纸下自动切换配色，保证对比度
- **ColorOS 质感**：小组件背景采用半透明大圆角容器，贴近 ColorOS 原生小组件观感，与系统桌面融为一体
- **添加预览**：提供 `previewImage`（低版本）与 `previewLayout`（API 31+），小组件列表里所见即所得
- P1 增加 2x2（纯图标）尺寸

### 4.3 记录追加面板（点心情图标后弹出，复用作编辑面板）

```
┌───────────────────────────────┐
│          ✅ 已记录             │
│          😄  很好              │
│                               │
│   今天的身体感觉（可多选）      │
│   [疲惫] [困倦] [不想动]       │
│   [没睡好] [头昏脑涨]          │
│   [肩颈酸痛] [眼睛干涩]        │
│   [胃部不适] [精力充沛]        │
│   [身体轻盈]                   │
│                               │
│          [ 完成 ]             │
└───────────────────────────────┘
```

- **身体感觉固定词表（10 项，面向职场人）**：疲惫、困倦、不想动、没睡好、头昏脑涨、肩颈酸痛、眼睛干涩、胃部不适、精力充沛、身体轻盈（前 8 项偏负面、后 2 项偏正面；P1 支持自定义增删）
- 点外部区域或"完成"即关闭；**未勾选任何项直接关闭也完全不影响记录**（记录在面板弹出时已落库）
- 面板顶部大图标给足"记录成功"的即时确认感
- **编辑形态**：从时间线/日历进入时，同一面板以编辑模式打开（预填该条记录的心跳与感觉，可改等级、改感觉，底部多一个红色"删除"按钮，删除需二次确认）

### 4.4 App 页面（底部 3 Tab + 设置入口）

- **首页**：今日日期 + 今日时间线（时间 + 心情 + 身体感觉标签，**点任意条目进编辑**）+ 大号记录按钮组（7 档）+ 近 7 日迷你趋势 +（未添加小组件时）引导卡片
- **日历**：月历网格按日均分着色（7 档色阶），点某天展开当日记录明细，**明细条目可点进编辑**
- **报告**：见 4.5
- **设置**（右上角入口）：主题 / 记录模式（面板 · 纯点击）/ **云同步（绑定网盘 · 同步开关 · 立即同步 · 上次同步状态，见 3.10）** / **数据导出（CSV · JSON）** / **清空全部数据（二次确认，含"是否同时删除云端备份"选项）** /（P1）应用锁、提醒、词表管理、导入 / 关于（含**《隐私政策》**入口、数据本地存储与云同步说明）

### 4.5 报告页信息架构（自上而下）

1. 月份切换器（‹ 2026年9月 ›）
2. **月评分卡**：大号评分 4.6 + 环比 ↑0.3 + 记录 26/30 天
3. **分布环形图**：7 色 + 各等级天数与占比
4. **趋势图**：日均分柱状 + 7 日移动平均线
5. **身体感觉卡**：Top 3 排行（出现天数）+ 心情 × 身体关联（"疲惫的日子平均低 1.4 分"）
6. **洞察卡**：最快乐的一天 / 心情最好的星期 / 连续记录天数
7. **建议卡**：规则引擎生成的 2~3 条建议
8. 页脚：文字总结 + 免责说明（必要时顶部插入关怀卡片，见 3.6.3）

### 4.6 空状态与无障碍

- **空状态**：首启首页显示引导文案（"点下面的图标，记下第一条心情吧"）+ 添加小组件引导；报告/日历在无数据月份显示轻量提示（插画 + 一句话），不渲染空图表
- **TalkBack**：所有心情图标配 `contentDescription`（如"很好，第 6 级"）；记录按钮、图表有语义标签
- **触控**：App 内所有可点目标 ≥ 48dp；4x2 主推即为此服务

### 4.7 视觉风格（对标 ColorOS 16「光场美学」）

整体对标 ColorOS 16 设计语言（光场美学 / Luminous Design）：**自然光影、半透明质感、轻盈通透、弹性动效**——目标是在 OPPO 手机上观感接近系统原生应用。

| 维度 | 规范 |
|---|---|
| 质感 | 半透明卡片 + 毛玻璃效果；Android 12+ 用 RenderEffect 实时模糊，API 26~30 自动降级为半透明纯色（视觉稍逊、功能无损） |
| 圆角 | ColorOS 式大圆角：卡片 24dp、小组件容器 28dp、按钮与图标容器 20dp |
| 光影 | 图标与卡片带柔和光晕描边、渐变高光，模拟 ColorOS「光影图标」的立体感；心情图标自绘时加"光边"处理 |
| 色彩 | 清新明快、低饱和柔和色阶；浅色底带暖调（米白）；Android 12+ 跟随系统动态取色，在 ColorOS 上自动联动 Flux 主题色 |
| 字体 | 不引入自定义字体，跟随系统字体（ColorOS 上即 OPPO Sans，天然统一） |
| 动效 | ① **"从哪来、到哪去"**：记录面板从点击图标位置缩放弹出、关闭时回缩归位；② **弹性物理动画**（spring 阻尼曲线）：记录成功时心情图标弹跳确认 + 心情色光晕扩散；③ 时长控制在 200~300ms，**绝不阻塞记录主路径** |
| 深色模式 | 半透明深色卡片同步设计，保持光感层次 |

技术备注：

- 光晕 / 立体感全部用**静态渐变资源**实现（径向渐变），不用实时模糊渲染，保证低端机也流畅
- 毛玻璃仅在 Android 12+ 开启，且只用于面板与卡片背景层
- M4 阶段在 OPPO 真机上逐项校验动效流畅度（目标 60fps）

### 4.8 品牌物料

| 物料 | 规范 |
|---|---|
| 正式名称 | **心晴**（英文名 SunnyMood，应用市场英文环境备用） |
| 包名 | `com.sunnymood.app`（发布后不再变更） |
| App 图标 | 自适应图标（Adaptive Icon）：前景为「含笑」情绪简笔图形（第 5 档心情的视觉母题），背景为晨曦渐变（暖橙→天蓝，呼应「晴」）；含 Android 13+ 单色主题图层 |
| 启动页 | 简洁品牌闪屏（图标 + 名称），Android 12+ 使用 SplashScreen API，冷启动 ≤ 400ms |
| 小组件预览 | 已在 §4.2 定义（previewImage / previewLayout） |

---

## 5. 开发计划

**执行方式（已确认）**：由 AI 直接产出**完整可编译的 Android 工程**（Kotlin + Gradle），交付后由你在 Android Studio 中打开、编译安装到真机验证，反馈问题后迭代。

| 里程碑 | 内容 | 完成标志（验收） | 参考工时* |
|---|---|---|---|
| **M1** 骨架 + 核心 | 工程搭建 / Room 数据层（含身体感觉字段、编辑删除 DAO）/ Widget 点击 → 面板（记录 + 身体感觉多选）→ 落库全链路 / 纯点击模式开关 | 桌面点图标 → 面板弹出且已落库 → 勾选感觉关闭 → 小组件状态刷新 | 3~4 天 |
| **M2** 首页 + 日历 + 编辑 | 今日时间线（含身体标签）/ 月历着色 / 记录编辑与删除（复用面板） | App 内记录、查看、改错、删除闭环 | 3 天 |
| **M3** 月报 + 建议 | 全部统计指标 / 身体排行与关联分析 / 建议引擎 / 关怀机制 | 任意月份报告 + 建议正确渲染 | 3~4 天 |
| **M4** 打磨 + 导出 + 合规 | 深色模式（含 Widget）/ 引导 / 空状态 / 动画 / 免责与措辞 / **数据导出（CSV · JSON）** / **隐私政策页与首启弹窗、清空全部数据** / 兼容性测试 | ROM 测试矩阵通过；§3.8 合规对照全项自查通过 | 3~4 天 |
| **M5** 云同步 | WebDAV 绑定与连接测试 / 加密引擎（口令派生 + AES-256-GCM）/ 快照上传下载 / 合并策略（UUID + LWW + 墓碑）/ 自动与手动同步 / 卸载重装恢复流 / 清空联动云端 | 坚果云真机全流程：加密上传 → 卸载重装 → 口令恢复 → 数据完整（UAT M5 全项通过） | 3~5 天 |

\* 按一名熟悉 Android 的全职开发者估算；AI 辅助开发可显著压缩。

- **M1 完成即可装机日用**，边用边开发后续里程碑
- **MVP（versionName 1.0.0）= M1~M5 全部完成**：v1.6 起云同步属 P0，纳入首版交付

### 5.1 测试策略

- **单元测试**：报告统计与建议引擎（纯函数，重点覆盖规则触发边界）、编辑/删除后的数据一致性、DAO 查询（内存数据库）
- **UI 测试**：Compose 测试核心记录流、面板交互、编辑与删除
- **兼容测试矩阵**：**OPPO（ColorOS 16，用户主力机，第一优先适配与日常实测）** / Pixel（原生）/ 小米 / 华为 / vivo 真机或云真机各一

### 5.2 构建与签名

| 项 | 方案 |
|---|---|
| 构建变体 | `debug`（日常联调，Android Studio 直接 Run）/ `release`（签名正式版，禁用调试特性） |
| 签名密钥 | `keytool` 生成 `sunnymood.jks`（RSA，有效期 30 年）；**jks 不入库（.gitignore）**，本地保存并由你另行备份（U 盘 / 网盘） |
| ⚠️ 密钥丢失后果 | 无法覆盖升级——只能卸载重装，**本地数据全部丢失** |
| 版本号 | `versionName` 语义化 Major.Minor.Patch（M1 交付 0.1.0，MVP 完成 = 1.0.0）；`versionCode` 每次发布 +1 单调递增 |
| 产物 | `./gradlew assembleRelease` → `app/build/outputs/apk/release/`，随附 SHA-256 校验值 |

### 5.3 代码交付与协作流程

- **仓库托管**：完整工程推送到私有仓库（GitHub / Gitee 任选，仓库名 `sunnymood-android`），每个里程碑结束推送一次，云端备份、历史可回溯
- **分支策略**（个人项目轻量化）：`main` 始终保持可编译可安装，`feature/*` 开发分支自测通过后合并
- **迭代循环**：

```
AI 完成里程碑 → 推送并打 tag（m1 / m2 / m3 / m4 / m5）
   → 你拉取代码，Android Studio 编译安装
   → 对照 §10 附录 UAT 清单逐项验收
   → 问题反馈（对话内 + 截图 / 诊断日志）
   → AI 修复推送 → 复验通过 → 进入下一里程碑
```

- **提交规范**：`feat:` / `fix:` / `docs:` / `chore:` 前缀 + 中文摘要

---

## 6. 风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| 国产 ROM 小组件兼容差异 | 中 | 真机矩阵测试；只用最基础的 RemoteViews 特性 |
| 面板增加记录摩擦 | 中 | 记录在弹出瞬间已完成、可跳过、可设置关闭——三层保险；M1 装机后据实际体验调默认值 |
| 7 图标在 4x1 下触控目标偏小 | 低 | 主推 4x2 大图标版；引导页默认推荐 4x2 |
| 用户不会添加小组件 | 中 | 首启动检测 + 图文引导 + App 内保底记录入口 |
| 建议措辞不当造成压力 | 中 | 措辞规范（3.6.3）+ M4 阶段逐条审阅文案 |
| 卸载 / 换机丢数据 | 低 | **云同步为主**（端到端加密，M5）+ 一键导出为离线兜底 + 设置页显著提示 |
| WebDAV 各网盘实现差异（坚果云限制 / 重定向 / 兼容性） | 中 | 只用最基础的标准方法（PUT / GET / PROPFIND / MKCOL）；内置连接测试；坚果云 + NextCloud 双实测 |
| 同步口令丢失 → 云端备份无法解密 | 中 | 设置页明示后果 + 开启时强制二次输入 + 建议用户抄写备份口令；本地数据不受任何影响 |
| 弱网 / 同步失败 | 低 | 失败即时提示 + 下次触发自动重试 + 手动「立即同步」；不做后台常驻重试（原则不变） |
| 误记污染统计数据 | 低 | P0 编辑与删除；编辑复用记录面板，实现成本低 |
| 数据库结构变更 | 低 | 预留字段 + Room 版本迁移规范 |

---

## 7. 发布与分发

| 渠道 | 说明 | 建议 |
|---|---|---|
| 直接安装 APK | 最快，个人使用 | **v1 首选** |
| GitHub Releases | 免费公开分发 | 备选 |
| Google Play | $25 注册费，需合规 | 视需求 |
| 国内应用商店 | 需 **App 备案（ICP，2023 年起施行）** 与软件著作权，周期长 | 暂缓 |

---

## 8. 决策记录

**2026-09-10 第一次确认：**

| # | 事项 | 结论 |
|---|---|---|
| 1 | 心情分级数 | ✅ **7 级**（非常差 ~ 非常好） |
| 2 | 一天可记录次数 | ✅ **多次，随时点**，按时间戳独立存储 |
| 3 | MVP 备注 / 标签 | ✅ **不含**，纯点击；P1 记录后可选追加 |
| 4 | 开发执行方式 | ✅ **AI 直接开发完整工程**，你负责编译与真机反馈 |
| 5 | 报告导出图片 | ✅ 放 P1 |
| 6 | 界面语言 | ✅ 简体中文 |
| 7 | App 正式名称 | ⏳ 待定（开发期暂用 MoodLog，随时可改） |

**2026-09-10 第二次确认（v1.1）：**

| # | 事项 | 结论 |
|---|---|---|
| 8 | 身体感觉 | ✅ **加入**：多选标签、可选追加（面板模式）、固定 10 项词表、可在设置关闭 |
| 9 | 月度建议 | ✅ **加入**：本地规则引擎 2~3 条 + 触发式关怀卡片（含心理援助热线 12356）+ 免责声明 |

**2026-09-10 第三次确认（v1.2，文档评审）：**

| # | 事项 | 结论 |
|---|---|---|
| 10 | 误记处理 | ✅ **完整编辑 + 删除（P0）**：时间线/日历点条目可改等级、改身体感觉、删除（删除需二次确认） |
| 11 | 隐私锁 | ✅ **放 P1**：BiometricPrompt 生物识别 / 锁屏 PIN |
| 12 | 额外记录入口 | ✅ **放 P1**：长按图标快捷方式 + 下拉快捷设置磁贴 |
| 13 | 数据导出 | ✅ **提前到 P0**：CSV / JSON 一键分享导出 |
| 14 | 年度热力图 | ❌ 暂不加入（后续有需要再评估） |

**2026-09-10 第四次确认（v1.3）：**

| # | 事项 | 结论 |
|---|---|---|
| 15 | UI 视觉风格 | ✅ **对标 ColorOS 16「光场美学」**：半透明质感、光影层次、大圆角、弹性无缝动效；OPPO（ColorOS 16）为第一优先适配机型 |

**2026-09-10 第五次确认（v1.4）：**

| # | 事项 | 结论 |
|---|---|---|
| 16 | 信息安全合规 | ✅ **符合工信部 App 个人信息保护要求**：新增隐私政策（首启弹窗 + 设置入口）、清空全部数据、权限申请三原则；逐项对照见 §3.8 |

**2026-09-10 第六次确认（v1.5，开发流程补全）：**

| # | 事项 | 结论 |
|---|---|---|
| 17 | App 正式名称 | ✅ **「心晴」**（英文名 SunnyMood；包名 `com.sunnymood.app`） |
| 18 | 代码交付方式 | ✅ **代码仓库托管**（GitHub / Gitee 私有仓库，每里程碑推送） |
| 19 | 崩溃排障 | ✅ **本地诊断日志**（P0，随 M1 交付；不记录心情内容，可一键导出） |
| 20 | 真机验收 | ✅ **UAT 检查单**（附录 §10，M1~M5 各一张，全项通过才算里程碑完成） |

**2026-09-10 第七次确认（v1.6，云同步方案）：**

| # | 事项 | 结论 |
|---|---|---|
| 21 | 卸载重装数据不丢 | ✅ **WebDAV 云同步**：绑定用户自选网盘（坚果云 / NextCloud 等）+ 端到端加密；零自建服务器、零账号体系、零数据类第三方 SDK（技术方案见 3.10） |
| 22 | 微信登录 | ❌ **放弃**：微信开放平台移动应用仅对企业主体开放（需营业执照 + 认证费），个人开发者无法申请——硬门槛，非技术取舍 |
| 23 | 云同步版本归属 | ✅ **提前至 P0**（未采纳"放 P1"建议）：纳入 MVP 首版，新增 M5 里程碑（+3~5 天）；连带调整：INTERNET 权限改为 P0 申请、数据模型改 UUID 主键 + updatedAt / deleted 字段、隐私政策新增云同步专章 |

---

## 9. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-10 | 初版定稿：7 级心情、多次记录、纯点击、AI 开发完整工程 |
| v1.1 | 2026-09-10 | 新增身体感觉维度（可选多选、固定词表）；报告新增身体感觉排行与心情×身体关联分析；新增月度建议引擎与关怀机制；记录链路改为"面板模式（默认）/ 纯点击模式（可选）"双模式 |
| v1.2 | 2026-09-10 | 通读评审：P0 新增记录编辑与删除、数据一键导出（CSV/JSON，SAF 免权限）；P1 新增应用锁、长按图标快捷方式、快捷设置磁贴；补充空状态设计、无障碍（TalkBack/色弱）、小组件深色适配与预览图、时区与身体感觉程度边界约定；DAO 增补 update/delete |
| v1.3 | 2026-09-10 | 视觉风格对标 ColorOS 16「光场美学」：半透明毛玻璃质感、大圆角、光影图标、弹性无缝动效规范；OPPO 提为第一优先适配机型 |
| v1.4 | 2026-09-10 | 新增 §3.8 信息安全与合规：工信部整治要点逐项对照表、《隐私政策》要点、权限申请三原则；P0 新增隐私政策（首启弹窗/设置入口）与清空全部数据；发布分发补充 App 备案（ICP）说明；M4 里程碑并入合规自查 |
| v1.5 | 2026-09-10 | 开发流程补全：定名「心晴」（SunnyMood，包名 com.sunnymood.app）；新增 §5.2 构建与签名、§5.3 代码交付与协作流程、§3.9 本地诊断日志、§4.8 品牌物料、§10 附录 UAT 验收清单（M1~M4） |
| v1.6 | 2026-09-10 | 云同步定案并提前至 P0：新增 §3.10（WebDAV + 端到端加密）与 M5 里程碑、M5 UAT 清单；数据模型主键改 UUID、新增 updatedAt / deleted（软删除墓碑）；INTERNET 权限改为 P0 申请（用途单一）；§3.8 合规对照与隐私政策相应改写；放弃微信登录（企业主体硬门槛）；清理 §8 / §9 历史重复段落 |

---

## 10. 附录：真机验收清单（UAT）

每个里程碑交付后，在 OPPO（ColorOS 16）真机上逐项勾选，**全项通过方视为里程碑完成**。

### M1 —— 核心记录链路

- [ ] 全新安装、启动无崩溃
- [ ] 桌面长按可找到小组件（4x1 / 4x2），预览图正常
- [ ] 添加 4x2 后，7 个心情图标清晰、无截断错位
- [ ] 点击任一心情图标 ≤ 1 秒弹出轻面板，伴随震动反馈
- [ ] 面板显示「已记录」及正确的心情名称
- [ ] 勾选多个身体感觉 → 点「完成」正常关闭
- [ ] 不勾选直接划走 / 点外部关闭，无异常
- [ ] 小组件「已记 N 条」计数 +1，最新心情图标高亮
- [ ] 连续记录 5 条以上，全部正常
- [ ] 设置中切换「纯点击模式」后，点击直接落库不弹面板
- [ ] 锁屏 10 分钟（进程被杀）后再点击，功能正常
- [ ] 重启手机后小组件仍正常记录

### M2 —— 首页 + 日历 + 编辑

- [ ] 首页显示今日日期与时间线（时间 + 心情 + 身体感觉标签）
- [ ] App 内大按钮记录与 Widget 记录等效
- [ ] 近 7 日迷你趋势渲染正常
- [ ] 日历当月网格着色与实际记录一致
- [ ] 点任意日期展开当日明细
- [ ] 点时间线条目进入编辑面板，预填正确
- [ ] 修改心情等级保存后，首页与小组件状态同步刷新
- [ ] 修改身体感觉保存正常
- [ ] 删除单条（二次确认）后，列表与统计同步更新
- [ ] 无记录时显示空状态引导，无空白图表
- [ ] 跨天记录在日历按本地时区 0 点正确分格

### M3 —— 月报 + 建议

- [ ] 报告页默认当月，月份切换器可正常前后翻
- [ ] 月评分、记录天数与手工核算一致
- [ ] 分布环形图 7 色与各等级天数正确
- [ ] 趋势图柱状 + 7 日均线正常，无数据日处理合理
- [ ] 身体感觉 Top 3 排行正确
- [ ] 心情 × 身体关联数值方向合理（如疲惫日均分低于整体）
- [ ] 洞察卡三项（最快乐一天 / 最佳星期 / 连续天数）正确
- [ ] 建议卡 0~3 条，触发条件与数据吻合、措辞无评判性
- [ ] 空月份显示空状态提示
- [ ] 构造连续低落数据，关怀卡片出现且含热线 12356
- [ ] 页脚免责说明完整

### M4 —— 打磨 + 导出 + 合规

- [ ] 深色模式跟随系统，全部页面与小组件正常
- [ ] 首启隐私政策弹窗：不默认勾选、同意后不再弹、设置页有常驻入口
- [ ] 未添加小组件时引导卡片出现，添加后消失
- [ ] 导出 CSV：内容完整，Excel 打开中文无乱码
- [ ] 导出 JSON：结构正确可解析
- [ ] 清空全部数据：二次确认后执行，全页面回到空状态
- [ ] 诊断日志导出：zip 生成，含崩溃与操作日志
- [ ] 动效（面板弹出回缩、图标弹跳）流畅无掉帧
- [ ] TalkBack 可朗读心情图标的等级含义
- [ ] 系统大字体模式下无布局崩坏
- [ ] §3.8 合规对照表逐项自查通过
- [ ] APK 体积 ≤ 10MB
- [ ] release 签名包可覆盖安装升级（versionCode 递增生效）

### M5 —— 云同步（WebDAV + 端到端加密）

**绑定与配置**

- [ ] 设置页进入云同步：坚果云服务器地址预置，输入账号与应用密码后连接测试通过
- [ ] 首次开启弹出同步口令设置：二次输入确认 +「口令丢失将无法恢复云端数据」明示
- [ ] 未开启同步时，飞行模式全程可用；抓包验证 App 零网络请求

**加密与上传**

- [ ] 开启同步后记录一条心情，云端 `sunnymood/` 目录出现加密备份文件
- [ ] 从网页端下载该文件用文本编辑器打开：看不到任何明文（心情数值、时间、身体感觉全部为密文）
- [ ] 同步状态行正确显示上次同步时间与结果（成功 / 失败原因）

**卸载重装恢复（核心场景）**

- [ ] 卸载 → 重装 → 绑定同一网盘账号 + 同一同步口令 → 记录完整恢复（条数、时间、心情、身体感觉逐一核对）
- [ ] 输入错误口令：提示解密失败、不崩溃、可重试
- [ ] 恢复完成后 Widget「已记 N 条」与首页时间线立即正确

**合并与删除传播**

- [ ] 重装前记 3 条 → 同步；重装后恢复再记 2 条 → 同步：5 条共存、无丢失无重复
- [ ] 一端删除某条记录 → 同步 → 另一端该记录不复活
- [ ] 「清空全部数据」提供「是否同时删除云端备份」选项，两种选择行为均正确

**异常路径**

- [ ] 断网时手动同步：失败提示清晰、不崩溃；恢复网络后手动 / 自动重试成功
- [ ] 网盘应用密码错误 / 服务器地址失效：错误信息可读，可重新配置

---

*本文档当前版本 v1.6，确认后冻结为开发基线。*
